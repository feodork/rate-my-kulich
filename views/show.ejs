<%- include ("./partials/head.ejs") %>
<div class="container">
<div class="flex-row">
  <img class="rounded-circle" src="<%= currentUser.profilePic %>">
  <div>
    <h2><%= theKulich.username.username %></h2>
  </div>
  </div>

<div class="container">
  <img class="img-fluid" src="<%= theKulich.image %>" />
</div>

<div class="container">
    <div class="flex-row">
      <form action="<%= baseUrl %>/<%= theKulich.id %>?_method=DELETE" method="post">
        <input class="btn btn-light" type="submit" value="Delete" />
      </form>

      <form class="button" action="<%= baseUrl %>/<%= theKulich.id %>/edit">
        <input class="btn btn-light" type="submit" value="Edit" />
      </form>
    </div>
</div>
<%- include ("./partials/footer.ejs") %>